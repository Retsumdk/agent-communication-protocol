{
  "name": "agent-communication-protocol",
  "description": "Inter-agent message passing protocol with delivery guarantees, acknowledgment receipts, and priority queuing",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "test": "bun test",
    "build": "tsc"
  },
  "dependencies": {
    "commander": "^11.1.0"
  },
  "devDependencies": {
    "bun-types": "^1.0.0",
    "typescript": "^5.3.0"
  }
}